!!!
%html
  = render 'layouts/head'
  %body
    #header-layout
      = yield :header
    #body-layout.container
      = yield :container
    = yield :footer
    -# = javascript_include_tag 'shared'
    = yield :footer_scripts
